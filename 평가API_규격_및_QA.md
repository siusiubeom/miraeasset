# 평가용 API 규격 및 운영진 Q&A 정리

> 출처: 대회 디스코드 공지 및 운영진(damien) 답변 (2026-08-11) + **API 호출 관련 공식 공지 (2026-08-14)**
> ✅ 8/14 공식 공지로 아래 규격 대부분이 **확정**됨. 남은 공지 대기 항목은 §7 참고.

---

## 1. 평가용 API End-point 제출

- 각 참가팀은 네이버클라우드 **또는 개인 서버자원**에 Agent를 API 형태로 구성
- 서버 환경은 자유이나, **Public 망에서 접근 가능한 네트워크 구성 필수**
- **API End-point 주소는 README.md 파일에 명시 (필수)**
- 주최측 평가 담당자가 제출된 End-point에 **GET 요청**으로 비공개 평가질의를 전송하고, 응답을 수집하여 평가

## 2. 평가 호출 방식

| 항목 | 내용 |
|---|---|
| 호출 방식 | **순차(sequential)** — 팀당 1건씩, **동시 요청 없음** (확정) |
| 타임아웃 | 문항당 **300초** (8/14 공지로 확정) |
| 재시도 | 타임아웃 또는 **5xx 오류** 발생 시 **최대 2회** 재시도 (8/14 공지로 확정) |

## 3. 호출/응답 규격

### 3-1. 요청 (주최측 → 참가팀)

```
GET {team-end-point}/answer?question_id={질의 ID}&question={평가 질의}
```

- 경로는 **`/answer`로 고정**
- 요청에 **별도 헤더 없음 (인증 헤더 포함 없음)** — 우리 서버가 요청자를 검증할 인증 수단 없음
- 접근 제어가 필요하면 **주최측 발신 IP 허용(allowlist)** 방식 활용 (발신 IP 대역은 추후 공지)

### 3-2. 응답 (참가팀 → 주최측, JSON)

```json
{
  "question_id": "Q-001",
  "question": "평가 질의 원문",
  "retrieved_context": "답변 생성에 참고한 검색 문서",
  "think_trace": "사고, 추론, 도구 사용 과정",
  "answer": "최종 생성 답변"
}
```

- **모든 필드 값은 문자열(string) 타입** (운영진 재확인)
- `retrieved_context`에 여러 문서가 있으면 문자열 안에서 구분 태그 등으로 자유롭게 연결 — **어떤 구분 태그를 쓰는지는 평가 대상 아님**
- 응답은 **`application/json`** 으로 반환
- 필드 길이 제한 없음. 단, 극단적으로 길면 평가 시스템 처리 한도 초과분은 평가에 미반영될 수 있음
- **빈 문자열 반환 허용** (`retrieved_context`, `think_trace` 등) — 문제되지 않음
- `think_trace` **형식 규정 없음** (길이·구조 자유), string 타입만 지키면 됨

## 4. 서버 구성

| 항목 | 내용 |
|---|---|
| 프로토콜 | **HTTP 기본**, 참가팀 판단에 따라 HTTPS 구성도 무방 (**자체 서명 인증서(self-signed) 허용**) — 8/14 공지로 확정 |
| 포트 | HTTP → **80**, HTTPS → **443** (표준 포트) |
| 도메인 | 불필요. **공인 IP 제출로 충분** |
| 발신 IP | 주최측 발신 IP 대역 추후 공지. 원하면 방화벽에서 해당 대역만 허용 가능 |
| 운영 기간 | **09.07 ~ 09.20 중 별도 공지 기간** (주제별 최대 1주 이내, 정확한 일자 추후 공지) — 8/14 공지로 재확인<br>※ 과제소개자료의 예선 평가 기간 09.07~09.30과 상이 — 운영진은 "별도 공지 예정"으로 답변 |
| 재기동 | 제출 마감 이후, '제출 결과물의 변경'이 아닌 불가피한 사유(서버 장애 등)에 따른 **서버 재기동은 실격 사유 아님** (8/14 공지 명문화) |
| 평가 시간대 | 평가 서버를 띄우는 시간은 공지 예정 (예: 00.00일~00.00일 중 09:00–16:00 간) — **기간 전체 상시 가동이 전제가 아닐 수 있음** |
| 개인 환경 | 노트북 등 개인 환경 구성 시, 외부 접근 가능한 정적 도메인 URL 제출. ngrok 등 터널링 서비스 자유롭게 활용 가능 |

## 5. 요청 예시 (주최측 제공)

```python
import requests

# 도메인 없이 공인 IP 직접 접근 가능, 표준 포트(HTTP 80 / HTTPS 443) 사용 시 포트 표기 생략
URL = "http://123.45.67.89/answer"

params = {
    "question_id": "Q-001",
    "question": "평가 질의",
}

resp = requests.get(URL, params=params, timeout=300)
result = resp.json()
```

```bash
curl -G "http://123.45.67.89/answer" \
  --data-urlencode "question_id=Q-001" \
  --data-urlencode "question=평가 질의"
```

---

## 6. 운영진 Q&A 답변 정리 (damien, 2026-08-11)

### 평가 호출
1. **순차 호출 확정** — 평가 질의는 한 건씩 순서대로 (동시 요청 없음)
2. **타임아웃 300초** — ✅ 8/14 공지로 확정
3. **에러 시 재시도 2회** — ✅ 8/14 공지로 확정

### 서버 운영
4. **장애로 인한 재기동은 실격 아님** — ✅ 8/14 공지에 명문화됨
5. HTTPS/HTTP 여부 — ✅ 8/14 공지로 확정: HTTP 기본, HTTPS(자체 서명 포함)도 무방
6. **평가 서버 가동 시간대는 공지 예정** (예: 특정 기간 중 09:00–16:00) → 상시 가동 여부는 공지 확인 후 결정
7. 서버 운영 기간 — 09.07~09.20 중 별도 공지 (주제별 최대 1주 이내, 정확한 일자 추후 공지)

### 비용
8. **제공 크레딧은 VAT(부가세) 포함**, 네이버클라우드 자원 사용 **전반**에 적용 (CLOVA Studio + 서버 + 네트워크 등 모두 차감)

### 기술 제약
9. HyperCLOVA 모델 사용 제한사항 — **공지사항 참고** (전처리 단계 예외 여부는 명확한 답변 없음, 추가 확인 필요)
10. **형태소 분석기·규칙 기반 NLP 라이브러리는 사용 제한 없음** (Mecab, Kiwi 등 OK)

### 평가 내용
11. 코퍼스로 답할 수 없는 질의 → **'정보한계대응' 평가항목 참고** (= 답 불가 질의가 포함됨을 시사)

### 제출물
12. **전처리 산출물(파싱 결과·인덱스)도 제출 대상** — 대용량은 클라우드 스토리지 링크로 대체 가능
13. **Dockerfile 필수 아님** — 단, 재현 가능한 환경 구성 정보(requirements.txt 등)는 제출 필수
14. **기술제안서 분량·양식 제한 없음**

### 마감
15. **제출 마감: 09.06(토) 23:59 KST**

---

## 7. 미해결 / 추후 공지 대기 항목

- [x] ~~API 관련 추가 공지~~ → 8/14 공식 공지 수신 (본 문서에 반영 완료)
- [x] ~~HTTPS vs HTTP 최종 확정~~ → HTTP 기본, HTTPS(자체 서명 포함) 무방으로 확정
- [ ] 주최측 발신 IP 대역 (8/14 공지에서도 "추후 공지 예정")
- [ ] 평가 서버 가동 정확한 기간·시간대
- [ ] 서버 운영 기간 확정 (09.07~09.20 vs 09.07~09.30 불일치 해소)
- [ ] 전처리 단계에서 타 LLM 사용 허용 여부 (공지사항 재확인 필요)
- [ ] 임베딩 모델·리랭커가 'LLM 제한'에 포함되는지 (답변 없음 — 재질문 필요)
- [ ] 마감 이후 SSH 접속(재부팅·로그 확인 목적) 허용 범위 (명시 답변 없음 — 4번 답변상 장애 대응은 허용으로 해석)

## 8. 우리 서버 설계에 주는 시사점

- **동시성 처리 불필요** — 순차 1건씩이므로 단일 워커로 충분. 다만 재시도(동일 question_id 재수신) 대비 **멱등 처리/캐싱** 고려
- **300초 안에 반드시 응답** — 파이프라인 전체(검색+추론+생성)에 타임아웃 가드 필요. 300초 임박 시 부분 답변이라도 반환하는 fallback 권장
- **5xx를 내지 말 것** — 내부 예외 발생 시에도 200 + 성실한 JSON(빈 문자열 허용)으로 응답하는 게 안전
- **인증 헤더가 없으므로** 공개된 엔드포인트임 — 발신 IP 공지 후 방화벽 allowlist 적용 권장
- 평가 시간대가 공지되면 **그 시간대에만 서버를 켜는 방식으로 크레딧 절약 가능**할 것으로 보임 (공지 대기)
